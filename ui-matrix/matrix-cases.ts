export type UiMatrixProductTarget = 'desktop' | 'browser-app' | 'demo-browser' | 'mobile';
export type UiMatrixDataState = 'fresh' | 'autogenerated' | 'dependent';
export type UiMatrixPriority = 'P0' | 'P1' | 'P2';
export type UiMatrixTestType = 'smoke' | 'regression' | 'visual' | 'interaction' | 'stateful' | 'end-to-end';

export interface UiMatrixCase {
  id: string;
  productTarget: UiMatrixProductTarget;
  dataState: UiMatrixDataState;
  area: string;
  initialData: string;
  interaction: string;
  expectedUi: string;
  expectedState: string;
  consistency: string;
  priority: UiMatrixPriority;
  testTypes: UiMatrixTestType[];
  captures: string[];
  edgeNotes: string;
}

export const UI_MATRIX_CASES: UiMatrixCase[] = [
  {
    id: 'desktop-fresh-first-run-empty-routes',
    productTarget: 'desktop',
    dataState: 'fresh',
    area: 'Onboarding, empty workspace, guarded routes',
    initialData: 'Isolated empty desktop data directory with no generated history.',
    interaction: 'Complete onboarding, navigate empty routes, open disabled or empty surfaces, resize and scroll.',
    expectedUi: 'No crash, no NaN, no broken charts, no document overflow, and empty-state guidance remains visible.',
    expectedState: 'Preferences persist onboarding completion while catalog, observations, and order batches remain empty.',
    consistency: 'Bridge counts agree with empty route state after reload.',
    priority: 'P0',
    testTypes: ['smoke', 'visual', 'interaction', 'stateful'],
    captures: ['onboarding', 'home', 'catalog-new-sku', 'work', 'settings-history'],
    edgeNotes: 'Fresh route guards may redirect unavailable pages to create/setup routes.',
  },
  {
    id: 'desktop-generated-route-and-control-sweep',
    productTarget: 'desktop',
    dataState: 'autogenerated',
    area: 'Generated workspace route sweep and major controls',
    initialData: 'Deterministic generated history fixture.',
    interaction: 'Visit major routes, open detail pages, use search/filter/dropdown controls, open drawers where data allows.',
    expectedUi: 'Populated tables, charts, cards, drawers, and details remain stable with captures.',
    expectedState: 'Generated catalog, observation, summary, and detail data remain readable across route changes.',
    consistency: 'Catalog counts and first detail ids match bridge probes across Catalog and detail pages.',
    priority: 'P0',
    testTypes: ['regression', 'visual', 'interaction'],
    captures: ['home', 'work-queue', 'catalog', 'sku-detail', 'service-detail', 'insights'],
    edgeNotes: 'Run medium by default; heavier fixtures can be selected by environment or focused command.',
  },
  {
    id: 'desktop-generated-sku-detail-chart-regressions',
    productTarget: 'desktop',
    dataState: 'autogenerated',
    area: 'Generated SKU detail chart layout and chart ledger labels',
    initialData: 'Deterministic generated history fixture with a populated SKU detail chart.',
    interaction: 'Open a generated SKU detail page and inspect the chart window, flag overlay bounds, and chart ledger eyebrow.',
    expectedUi: 'The non-expanded chart keeps a content-height layout, offscreen flag buttons are not rendered, and the chart ledger eyebrow says Chart View rather than using the Khmer brand mark.',
    expectedState: 'Opening the detail chart does not mutate generated catalog, observations, or chart preference state.',
    consistency: 'Chart detail assertions run inside the same generated route sweep used for Catalog and Insights parity.',
    priority: 'P0',
    testTypes: ['regression', 'visual'],
    captures: ['sku-detail-chart'],
    edgeNotes: 'Complements unit tests that control pane counts and mocked coordinates more precisely.',
  },
  {
    id: 'desktop-dependent-organic-update-chain',
    productTarget: 'desktop',
    dataState: 'dependent',
    area: 'Organic catalog and record-update state propagation',
    initialData: 'Fresh workspace with onboarding skipped as setup only.',
    interaction: 'Create SKU and service through UI, edit SKU pricing, save stock count through UI, reload and cross-check routes.',
    expectedUi: 'New entities and updates appear in Catalog, detail, Work/Capture, Inventory, and Logs without stale state.',
    expectedState: 'Catalog and observation counts increase; saved values persist after reload.',
    consistency: 'Bridge probes agree with visible entity names and observation/history surfaces.',
    priority: 'P0',
    testTypes: ['stateful', 'end-to-end', 'regression', 'visual'],
    captures: ['sku-form', 'service-form', 'stock-count-review', 'catalog-after-reload', 'history'],
    edgeNotes: 'Order/receipt subflows can extend this chain without changing helper contracts.',
  },
  {
    id: 'desktop-dependent-ticket-lifecycle-chain',
    productTarget: 'desktop',
    dataState: 'dependent',
    area: 'Customer and supplier ticket lifecycle updates',
    initialData: 'Organic workspace after SKU/service creation and first pending tickets.',
    interaction: 'Create pending customer and supplier tickets, update existing tickets, change ETA, complete/receive when available, verify stock deltas, then revisit task surfaces.',
    expectedUi: 'Ticket dialogs, review modals, queue rows, history, and detail pages stay usable while the ticket state evolves.',
    expectedState: 'Ticket revisions append without losing original lines, stages advance, receipt/completion quantities remain tied to the same entities, and stock snapshots reflect sale/receipt deltas.',
    consistency: 'Home, Work queue, Catalog detail, History, Inventory, Money, and Explain agree on the same ticket-backed changes.',
    priority: 'P0',
    testTypes: ['stateful', 'end-to-end', 'regression'],
    captures: ['customer-ticket-update', 'supplier-ticket-update', 'supplier-receipt', 'queue-after-ticket-updates'],
    edgeNotes: 'Focused UI steps should extend the dependent spec as the ticket editor controls stabilize.',
  },
  {
    id: 'desktop-dependent-restart-persistence',
    productTarget: 'desktop',
    dataState: 'dependent',
    area: 'Desktop close/reopen persistence',
    initialData: 'Organic workspace after user-created catalog and observations.',
    interaction: 'Close the Electron app, reopen against the same data directory, and revisit major surfaces.',
    expectedUi: 'Previously created entities and observations render after a true desktop restart without onboarding or stale loading states.',
    expectedState: 'Catalog, observations, tickets, order batches, and preferences persist on disk across the process boundary.',
    consistency: 'Bridge reads after relaunch match visible Catalog, detail, History, and Work queue state.',
    priority: 'P0',
    testTypes: ['stateful', 'regression', 'end-to-end'],
    captures: ['catalog-after-desktop-restart', 'history-after-desktop-restart'],
    edgeNotes: 'Renderer reload alone is not enough evidence for this case.',
  },
  {
    id: 'browser-app-fresh-empty-workspace',
    productTarget: 'browser-app',
    dataState: 'fresh',
    area: 'Browser app onboarding and empty local-first workspace',
    initialData: 'Reset browser app storage with no SKUs, services, observations, orders, tickets, or history.',
    interaction: 'Open onboarding, complete setup, inspect empty Home, Catalog/create, Work, Local data, and backup controls.',
    expectedUi: 'Browser app empty states, local-first banner, reset/import/export controls, and forms remain stable.',
    expectedState: 'Browser profile workspace remains empty until the user creates data and survives route changes.',
    consistency: 'Browser bridge counts agree with empty visible surfaces.',
    priority: 'P1',
    testTypes: ['smoke', 'interaction', 'stateful', 'visual'],
    captures: ['web-app-onboarding', 'web-app-empty-home', 'web-app-empty-local-data'],
    edgeNotes: 'OPFS support may vary, but fallback UI still must be stable.',
  },
  {
    id: 'demo-browser-fresh-reset-state',
    productTarget: 'demo-browser',
    dataState: 'fresh',
    area: 'Demo reset and empty/demo-safe surfaces',
    initialData: 'Demo browser route after explicit reset.',
    interaction: 'Use Reset demo, confirm banner controls, route through representative empty or reseeded demo surfaces.',
    expectedUi: 'Demo reset does not break banners, routes, or local-first controls.',
    expectedState: 'Demo state stays isolated from browser-app state.',
    consistency: 'Demo Home, Catalog, Work queue, and History agree after reset.',
    priority: 'P1',
    testTypes: ['smoke', 'interaction', 'visual'],
    captures: ['web-demo-after-reset', 'web-demo-history-after-reset'],
    edgeNotes: 'Demo may reseed by design; the assertion is isolation and stability rather than zero rows.',
  },
  {
    id: 'web-demo-and-browser-parity',
    productTarget: 'demo-browser',
    dataState: 'autogenerated',
    area: 'Demo and browser app banners, local-first controls, major route parity',
    initialData: 'Browser embedded runtime fallback/demo state.',
    interaction: 'Open /demo and /app, inspect banners, reset/import/export controls, navigate populated routes.',
    expectedUi: 'Demo/browser banners and controls remain visible, routes render, and local-first copy is present.',
    expectedState: 'Demo state stays separate from browser-app state.',
    consistency: 'Major routes expose the same navigation families as desktop where supported.',
    priority: 'P1',
    testTypes: ['smoke', 'visual', 'interaction'],
    captures: ['demo-home', 'demo-catalog', 'app-onboarding-or-home'],
    edgeNotes: 'Browser storage support is environment-dependent; fallback state is still a valid UI regression target.',
  },
  {
    id: 'web-landing-primary-release-actions',
    productTarget: 'demo-browser',
    dataState: 'fresh',
    area: 'Public landing primary CTA and release download controls',
    initialData: 'Public GitHub Pages landing page before entering demo or browser app mode.',
    interaction: 'Inspect the hero Get started CTA and, when release metadata loads, the selected download control.',
    expectedUi: 'Primary landing actions use the shared gleam treatment and the release selector keeps the wider responsive width for long asset labels.',
    expectedState: 'Landing-page inspection does not enter or mutate embedded demo/browser storage.',
    consistency: 'Landing controls keep the same action treatment before opening the embedded app routes.',
    priority: 'P1',
    testTypes: ['regression', 'visual'],
    captures: ['web-landing-entry'],
    edgeNotes: 'Release metadata can be unavailable offline; the download assertion is conditional on the control rendering.',
  },
  {
    id: 'web-autogenerated-populated-surface-sweep',
    productTarget: 'demo-browser',
    dataState: 'autogenerated',
    area: 'Populated browser/demo details, insights, history, and controls',
    initialData: 'Seeded demo/browser data with catalog and observations.',
    interaction: 'Visit Catalog, item detail, Inventory, Money, Explain, History, Work queue, and common filters/dropdowns.',
    expectedUi: 'Populated browser surfaces keep parity with desktop layout integrity and interaction stability.',
    expectedState: 'Seeded entities and observations remain readable through the browser bridge.',
    consistency: 'Entity names and counts remain consistent between Catalog, detail, insights, and history surfaces.',
    priority: 'P1',
    testTypes: ['regression', 'visual', 'interaction'],
    captures: ['web-demo-sku-detail', 'web-demo-inventory', 'web-demo-money', 'web-demo-history'],
    edgeNotes: 'Does not need desktop-depth fixture-size permutations.',
  },
  {
    id: 'mobile-responsive-embedded-layouts',
    productTarget: 'mobile',
    dataState: 'autogenerated',
    area: 'Phone portrait, phone landscape, and narrow responsive shells',
    initialData: 'Embedded demo/browser runtime at mobile viewports.',
    interaction: 'Resize to phone portrait and landscape, verify the portrait operator shell, phone product lookup/detail for SKUs and services, capture entry for Products Update, Customer Orders Pending/Completed, Supplier Orders Pending actions, saved-draft deletion from detail actions, safety controls, and responsive workspace.',
    expectedUi: 'No incoherent overlap or document overflow; portrait operator shell, Insights tab, safety affordance, SKU/service detail, saved-draft prompt, capture entry actions, and landscape workspace remain stable.',
    expectedState: 'Responsive changes do not reset browser/demo workspace state.',
    consistency: 'Route hash survives viewport changes.',
    priority: 'P1',
    testTypes: ['visual', 'interaction', 'regression'],
    captures: ['mobile-phone-portrait-setup', 'mobile-phone-portrait-shell', 'mobile-phone-portrait-more-safety', 'mobile-phone-portrait-product-detail', 'mobile-phone-portrait-service-detail', 'mobile-phone-portrait-capture-menu', 'mobile-phone-portrait-stock-count', 'mobile-phone-portrait-supplier-order', 'mobile-phone-portrait-immediate-sale', 'mobile-phone-portrait-customer-order', 'mobile-phone-landscape-demo'],
    edgeNotes: 'Pixel baselines are intentionally deferred; captures are diagnostic artifacts.',
  },
  {
    id: 'mobile-fresh-empty-responsive-layouts',
    productTarget: 'mobile',
    dataState: 'fresh',
    area: 'Fresh phone/tablet onboarding and empty states',
    initialData: 'Fresh embedded browser app state at phone and tablet breakpoints.',
    interaction: 'Resize to portrait, landscape, and tablet; complete onboarding and inspect empty forms/routes.',
    expectedUi: 'Phone setup, portrait operator shell, and tablet empty states avoid overlap, clipped controls, and horizontal overflow.',
    expectedState: 'Fresh browser-app state remains empty across viewport transitions.',
    consistency: 'Route hash and browser bridge counts survive resize transitions.',
    priority: 'P1',
    testTypes: ['visual', 'interaction', 'stateful'],
    captures: ['mobile-fresh-tablet-home', 'mobile-fresh-landscape-empty-work'],
    edgeNotes: 'Phone portrait intentionally shows the operator shell instead of the dense desktop shell.',
  },
  {
    id: 'mobile-dependent-browser-app-mutation',
    productTarget: 'mobile',
    dataState: 'dependent',
    area: 'Tablet browser app mutation, backup, restore, and persistence',
    initialData: 'Fresh browser app at tablet breakpoint.',
    interaction: 'Create a SKU through tablet controls, route away/back, reload, export a backup, reset the workspace, import the backup, and verify state returns.',
    expectedUi: 'Tablet forms, detail page, Catalog, and backup/import/reset controls remain stable after mutation, reload, reset, and import.',
    expectedState: 'Created SKU persists in browser storage across reload and returns after backup import.',
    consistency: 'Browser bridge catalog count and visible Catalog/detail text agree after mutation, reset, and backup import.',
    priority: 'P1',
    testTypes: ['stateful', 'interaction', 'regression', 'visual'],
    captures: ['mobile-dependent-sku-created', 'mobile-dependent-after-reload', 'mobile-dependent-after-backup-import', 'mobile-dependent-phone-app-safety', 'mobile-dependent-phone-after-backup-import'],
    edgeNotes: 'Complements desktop-dependent coverage; browser app storage is the persistence boundary.',
  },
];
